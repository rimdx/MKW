<UserControl x:Class="MKW.GUI.EntryEditor.PayloadEditorGeneral"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MKW.GUI.EntryEditor"
             xmlns:gui="clr-namespace:MKW.GUI"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:EntryEditorViewModel}"
             d:DesignHeight="380" d:DesignWidth="520"
             d:Background="White">
  <UserControl.Resources>
    <Style TargetType="{x:Type Label}">
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0,4,18,0" />
    </Style>

    <Style TargetType="{x:Type TextBox}">
      <Setter Property="Padding" Value="3" />
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
    <Style TargetType="{x:Type gui:PasswordInput}">
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
  </UserControl.Resources>

  <Grid Margin="12,12,12,6">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Label Content="Title:"
           Grid.Row="0"
           Target="{Binding ElementName=TitleInput}" />
    <TextBox x:Name="TitleInput"
             Grid.Column="1"
             Grid.Row="0"
             Text="{Binding Properties[mkw:title].Value,
                            UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

    <Label Content="User name:"
           Grid.Row="1"
           Target="{Binding ElementName=UsernameInput}" />
    <TextBox x:Name="UsernameInput"
             Grid.Column="1"
             Grid.Row="1"
             Text="{Binding Properties[mkw:username].Value,
                            UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

    <Label Content="Password:"
           Grid.Row="2"
           Target="{Binding ElementName=PasswordInput}" />
    <TextBox x:Name="PasswordInput"
             Grid.Column="1"
             Grid.Row="2"
             Text="{Binding Properties[mkw:password].Value,
                            UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

    <Label Content="URL:"
           Grid.Row="3"
           Target="{Binding ElementName=UrlInput}" />
    <TextBox x:Name="UrlInput"
             Grid.Column="1"
             Grid.Row="3"
             Text="{Binding Properties[mkw:url].Value,
                            UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

    <Label Content="Notes:"
           Grid.Row="4"
           Target="{Binding ElementName=NotesInput}" />

    <TextBox x:Name="NotesInput"
             Grid.Column="1"
             Grid.Row="4"
             Text="{Binding Properties[mkw:notes].Value,
                            UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
             AcceptsReturn="True" />
  </Grid>
</UserControl>
