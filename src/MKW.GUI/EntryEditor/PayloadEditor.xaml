<UserControl x:Class="MKW.GUI.EntryEditor.PayloadEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MKW.GUI.EntryEditor"
             xmlns:model="clr-namespace:MKW.GUI.Model"
             xmlns:gui="clr-namespace:MKW.GUI"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=model:EntryEditorModel}"
             d:DesignHeight="380" d:DesignWidth="520"
             d:Background="White">
  <Grid>
    <TabControl>
      <TabItem Header="General">
        <local:PayloadEditorGeneral FontWeight="Regular" />
      </TabItem>
      <TabItem Header="Properties">
        <local:PayloadEditorProperties FontWeight="Regular" />
      </TabItem>

      <TabControl.ContentTemplate>
        <DataTemplate>
          <Border BorderThickness="1"
                  BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
            <ContentPresenter Content="{Binding}" />
          </Border>
        </DataTemplate>
      </TabControl.ContentTemplate>
    </TabControl>
  </Grid>
</UserControl>
