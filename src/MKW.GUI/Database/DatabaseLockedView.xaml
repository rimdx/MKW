<UserControl x:Class="MKW.GUI.Database.DatabaseLockedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MKW.GUI.Database"
             xmlns:gui="clr-namespace:MKW.GUI"
             xmlns:images="clr-namespace:MKW.GUI.Images;assembly=MKW.GUI.Images"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:DatabaseLockedViewModel}"
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="White">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
      <StackPanel Orientation="Horizontal">
        <gui:CrispImage Moniker="Lock" Height="20" Margin="0,0,6,0"/>
        <TextBlock FontSize="20" FontWeight="SemiBold">
        Database is locked
        </TextBlock>
      </StackPanel>
      <TextBlock Margin="0,6,0,0">
        <Hyperlink Command="{x:Static gui:AppCommands.UnlockDatabase}">Unlock database...</Hyperlink>
      </TextBlock>
    </StackPanel>
  </Grid>
</UserControl>
