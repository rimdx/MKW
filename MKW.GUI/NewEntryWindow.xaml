<Window x:Class="MKW.GUI.NewEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MKW.GUI"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:NewEntryWindowViewModel}"
        Title="Create new secret entry"
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>

    <Label Grid.Row="0"
           Content="Entry content:"
           Target="Payload"
           Margin="0,0,0,3"
           Padding="0" />

    <TextBox Grid.Row="1"
             x:Name="Payload"
             Text="{Binding Payload, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
             FontFamily="Consolas"
             AcceptsReturn="True" />

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="auto" />
        <ColumnDefinition Width="auto" />
      </Grid.ColumnDefinitions>

      <Button Grid.Column="1"
              Content="OK"
              Width="70"
              Click="OK_Click"
              IsDefault="True"
              Margin="0,0,6,0" />

      <Button Grid.Column="2"
              Content="Cancel"
              Width="70"
              Click="Cancel_Click"
              IsCancel="True" />
    </Grid>
  </Grid>
</Window>
