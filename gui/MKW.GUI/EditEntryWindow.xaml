<local:DialogWindow x:Class="MKW.GUI.EditEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MKW.GUI"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:EditEntryWindowViewModel}"
        Title="Edit Entry"
        Height="300" Width="400">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>

    <Label Grid.Row="1"
           Content="Entry content:"
           Target="Payload"
           Padding="0"
           Margin="0,0,0,3" />

    <TextBox Grid.Row="2"
             Text="{Binding Payload, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
             x:Name="Payload"
             FontFamily="Consolas"
             AcceptsReturn="True"
             Margin="0,0,0,6" />

    <Grid Grid.Row="4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="auto" />
        <ColumnDefinition Width="auto" />
      </Grid.ColumnDefinitions>

      <Button Grid.Column="1"
              Content="OK"
              Width="70"
              Click="OK_Click"
              IsDefault="True"
              Margin="0,0,6,0" />

      <Button Grid.Column="2"
              Content="Cancel"
              Width="70"
              Click="Cancel_Click"
              IsCancel="True" />
    </Grid>
  </Grid>
</local:DialogWindow>
